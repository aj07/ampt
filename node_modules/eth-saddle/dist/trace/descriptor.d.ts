import { InversionMap } from './inversion';
import { StructLog } from '../trace';
import { Log } from './log';
export interface Desc {
    op: string;
    inputs: string[];
    outputs: string[];
}
export interface TraceInfo {
    lastLog?: Log;
    inv: InversionMap;
    sha: object;
}
export declare function augmentLogs(logs: StructLog[], constants: object): {
    logs: Log[];
    info: TraceInfo;
};
//# sourceMappingURL=descriptor.d.ts.map