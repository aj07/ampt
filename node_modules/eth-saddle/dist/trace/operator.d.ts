import { StructLog } from '../trace';
import { TraceInfo } from './descriptor';
export interface ExtendedInfo {
    inputs: string[];
    outputs: string[];
    desc: string;
}
export declare function describeOperation(log: StructLog, nextStack: string[], info: TraceInfo): {
    extended: ExtendedInfo;
    info: TraceInfo;
};
//# sourceMappingURL=operator.d.ts.map